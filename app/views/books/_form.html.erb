<%= form_for(@book) do |f| %>
  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2>Blad</h2>

      <ul>
      <% @book.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<% end %>
<p>
Chcesz zarezerwować: 
  <%= @book.name %>
</p>

<%= form_for(@book) do |f| %>

 <%= f.hidden_field :rezerwacja, :value => true %>

  <br>
Podaj imię i nazwisko
<br>
  <%= f.text_area :uzytkownik, :input_html => {:rows => 2, :cols => 20}, :class => "create_user", :id => "rez", :onsubmit => "return validateForm()", :validate => "true" %>
  <%= f.button :Zarezerwuj, :class => 'button',  :onchange => "return val()", :onsubmit => "return val()" %>
  <%end%>


<%= link_to 'Strona główna', books_path %>

